import { 
    daysProps, 
    climateProps, 
    dataProps 
} from "./dataProps";
import responseSendHTMLProps from "./responseSendErrorProps";

export {
    daysProps,
    climateProps,
    dataProps,
    responseSendHTMLProps
}